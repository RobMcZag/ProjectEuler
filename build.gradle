apply plugin: 'java'
apply plugin: 'eclipse'

sourceCompatibility = 1.6
group = 'com.rzagni'
version = '0.1'

jar { 
    manifest { 
        def manifestClasspath = configurations.runtime.collect { it.getName() }.join(',') 
        attributes 'Class-Path': manifestClasspath 
    } 
} 

jar {
    manifest {
        attributes 'Implementation-Title': 'ProjectEuler', 'Implementation-Version': '0.1', 'Main-Class': 'com.rzagni.projectEuler.P009_Pythagorean_triplet'
    }
}

repositories {
    mavenCentral()
}

dependencies {
    testCompile 'junit:junit:4.11'
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.11'
}